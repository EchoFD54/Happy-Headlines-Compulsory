
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: hhuser
      POSTGRES_PASSWORD: hhpass
    ports:
      - "5432:5432"
    volumes:
      - pg-data:/var/lib/postgresql/data

volumes:
  pg-data: